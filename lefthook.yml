pre-commit:
  commands:
    build:
      run: |
        gradle build
        mv build/libs/Cardinal-Extension.jar addon-api.jar
        git add addon-api.jar

    javadoc:
      run: |
        gradle javadoc
        git add docs
      env:
        JAVADOC_OUTPUT_DIR: ./docs
